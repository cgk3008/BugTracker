@model IEnumerable<BugTracker.Models.AdminIndexViewModel>

@{
	ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (var mod in Model)
{
	<p>@mod.User.FullName | @Html.ActionLink("Edit Roles", "EditUser", new { id = mod.User.Id })</p>
	<ul>
		@foreach (var name in mod.Roles)
		{

			<li>
				@name
			</li>
		}
	</ul>

}




IsUserInRole(string UserId, string Role)

ListUsersInRole(string Role)

ListUsersNotInRole(string Role)

ListRolesForUser(string UserId)

ListRolesNotForUser(string UserId)